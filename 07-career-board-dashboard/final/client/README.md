# CareerBoard React Client

I built this React client for my CareerBoard API. I use Redux Toolkit for account, job, filter, pagination, and statistics state.

## How I run it

I run `npm install` and `npm start` while developing. I run `npm run build` when I want the production assets consumed by the Express server.
