# Commerce Authentication Client

I built this focused React client to exercise login, protected session checks, and logout against my commerce API.

## How I run it

I run `npm install` and `npm start` while my API is available at `http://localhost:5000`. I run `npm run build` to verify my production bundle.
