# Account Access React Client

I built this React client to practice registration, email verification, login, password recovery, password reset, protected routes, and account state.

## How I run it

I run `npm install` and `npm start` while my account API is available. I run `npm run build` to create and verify my production bundle.
